# ── Server ────────────────────────────────────────────────────────────────
PORT=5000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000

# ── Database (Prisma / Postgres) ────────────────────────────────────────────
# Matches the docker-compose.yml postgres service.
DATABASE_URL="postgresql://business_os:business_os_dev_password@localhost:5432/business_os?schema=public"

# ── JWT ──────────────────────────────────────────────────────────────────
# Generate a strong secret, e.g.: openssl rand -base64 48
JWT_ACCESS_SECRET=changeme-access-secret
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d
JWT_REFRESH_TTL_REMEMBER_ME=30d

# ── Two-Factor Authentication ───────────────────────────────────────────────
# AES-256-GCM key used to encrypt TOTP secrets at rest. Must be a 32-byte
# value, base64 encoded, e.g.: openssl rand -base64 32
TWO_FA_ENCRYPTION_KEY=changeme-32-byte-base64-key
TWO_FA_ISSUER=Grow More

# ── Refresh Token Cookie ────────────────────────────────────────────────────
REFRESH_COOKIE_NAME=refresh_token
# Left blank: same-origin deployment, no explicit cookie domain needed.
REFRESH_COOKIE_DOMAIN=

# ── Rate Limiting ────────────────────────────────────────────────────────
# Left blank to use the in-memory rate-limit store (single-instance only).
# Set to a redis:// URL before horizontally scaling the API.
REDIS_URL=
